# wordpress-CPT-pagination
Create custom post type number pagination list. Its easy. 

Copy code from cpt-pagination.php to your functions.php then use the function **custom_pagination($your_query)**
